services:
  db:
    image: "postgres:18-alpine"
    container_name: "BSP-DB"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: bsp-username
      POSTGRES_PASSWORD: bsp-password
      POSTGRES_DB: bsp-database